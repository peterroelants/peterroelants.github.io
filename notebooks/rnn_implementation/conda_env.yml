# Install with:
# conda env create --file ./conda_env.yml && conda activate rnn_numpy_blog
# Remove with:
# conda deactivate && conda remove --name rnn_numpy_blog --all
name: rnn_numpy_blog
channels:
  - conda-forge
dependencies:
  - matplotlib=3.4.2
  - notebook=6.3.0
  - numpy=1.20.2
  - python=3.9.4
  - seaborn=0.11.1
  - watermark=2.2.0
